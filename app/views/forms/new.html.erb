<h1>New form</h1>

<% semantic_form_for(@form) do |f| %>
  <%= f.error_messages %>

  <%= render :partial => 'form', :object => f %>
  <%= f.commit_button %>
<% end %>

<%= link_to 'Back', forms_path %>